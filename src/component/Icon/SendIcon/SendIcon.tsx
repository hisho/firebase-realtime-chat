import { BaseIcon, BaseIconProps } from '@src/component/Icon/BaseIcon'
import { BiSend } from 'react-icons/bi'

export const SendIcon = (props: BaseIconProps) => {
  return <BaseIcon icon={BiSend} {...props} />
}
