import { BaseIcon, BaseIconProps } from '@src/component/Icon/BaseIcon'
import { MdVerified } from 'react-icons/md'

export const VerifiedIcon = (props: BaseIconProps) => {
  return <BaseIcon icon={MdVerified} {...props} />
}
